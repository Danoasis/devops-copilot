apiVersion: v1
kind: Service
metadata:
  name: devops-copilot
  labels: { app: devops-copilot }
spec:
  type: ClusterIP
  selector: { app: devops-copilot }
  ports:
    - name: http          # NAME matters: the ServiceMonitor references it (KB-007!)
      port: 8000
      targetPort: http
